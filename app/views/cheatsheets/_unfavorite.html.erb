<%= form_for(@cheatsheet.favorites.find_by(user_id: current_user.id, cheatsheet_id: @cheatsheet.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= hidden_field_tag :cheatsheet_id, @cheatsheet.id %>
  <%= f.submit "Unfavorite", class: "btn btn-primary" %>
<% end %>
