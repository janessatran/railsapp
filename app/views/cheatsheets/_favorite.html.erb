
<%= form_for(@cheatsheet.favorites.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :user_id, current_user.id %></div>
    <div><%= hidden_field_tag :cheatsheet_id, @cheatsheet.id %></div>
  <%= f.submit "Favorite", class: "btn btn-primary" %>
<% end %>